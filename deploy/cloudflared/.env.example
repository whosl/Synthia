# Cloudflare Tunnel token — get it from:
#   https://one.dash.cloudflare.com → Networks → Tunnels → <your tunnel> → Install and run
# Starts with "eyJ..." and is ~200 chars. NEVER commit the real value.
TUNNEL_TOKEN=REPLACE_WITH_YOUR_TOKEN
