# UART 收发器可编程逻辑器件软件研制任务书（PLDSDTD）

## 封面

| 属性 | 内容 |
|---|---|
| 文档标识及版本 | UART-DOC-001 / v2.0 candidate |
| 数据分类 | 待项目责任人确定；当前按非公开工程资料处理 |
| 编制/修订日期 | 2026-08-26 |
| 文档名称 | UART 收发器可编程逻辑器件软件研制任务书 |
| 编制单位 | Synthia Golden 候选项目（待授权单位确认） |
| 编写 | Agent 辅助拟制，待授权角色署名 |
| 审核 | 待系统、验证、质量角色审核 |
| 批准 | 未批准；待项目负责人决定 |

## 修改页

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| v2.0 candidate | 2026-08-26 | 依据 GJB 9764-2020 附录 B 将开发技术要求重构为 PLDSDTD | Agent 辅助拟制，待授权角色确认 |

## 目录

1. 范围
2. 引用文档
3. 运行环境要求
4. 任务要求
5. 接口关系
6. 关键性要求
7. 设计约束
8. 管理要求
9. 质量保证
10. 验收与交付
11. 维护

## 1 范围

### 1.1 标识

项目标识为 GOLDEN-UART；PLDS 名称为 UART 收发器，顶层标识 `uart_top`；候选目标器件为 Xilinx Kintex-7 `xc7k70tfbv676-1`；文档版本为 v2.0 candidate，无发布号、批准基线或产品基线。

### 1.2 系统概述

本候选为 Synthia FPGA 工程流程参考项目，实现 9600 bit/s、8N1、全双工 UART。外部同步逻辑按字节提交发送数据并捕获接收结果，PLDS 在一对异步串行线 `txd/rxd` 上转换帧格式。真实装备系统、任务来源和用途未提供，不能据此推断安全关键等级或交付资格。

### 1.3 文档概述

本文规定开发目的、任务、指标、接口、约束、质量和交付要求，是 UART-DOC-002 PLDSRS 的候选上游。任务要求使用 `UART-DRQ-*` 唯一标识，完整追踪见 UART-DOC-007。

## 2 引用文档

| 文档标识 | 标题/版本 | 作者/单位 | 日期/状态 |
|---|---|---|---|
| GJB 9764-2020 | 军用可编程逻辑器件软件文档编制规范 | 中央军委装备发展部 | 2020-06-18；仓库权威扫描件 |
| GJB 9432-2018 | 军用可编程逻辑器件软件开发通用要求 | 待受控版本确认 | 待质量角色复核 |
| UART-DOC-000 | 项目保证与裁剪说明 v2.0 | Synthia Golden 候选项目 | 2026-08-26；candidate |

## 3 运行环境要求

| 编号 | 要求 | 状态 |
|---|---|---|
| UART-DRQ-DEV-001 | 目标器件为 `xc7k70tfbv676-1`；设计不得超出器件资源。 | 候选 |
| UART-DRQ-ENV-001 | 开发、综合与实现工具为 Vivado 2021.1，仿真为 XSim；Icarus 只作交叉检查。 | 候选 |
| UART-DRQ-ENV-003 | 正式约束须包含有依据的时钟、引脚、I/O 电平及适用 I/O 时序；事实缺失时实现 fail-closed。 | 关键 |

温度、电压、功耗、器件等级、抗辐照和板级环境未提供，当前本节相应内容无可批准数据；缺口阻断确认测试、正式实现和交付，不能由 Agent 填入典型值。

## 4 任务要求

### 4.1 功能要求

| 编号 | 要求 | 关键度 |
|---|---|---|
| UART-DRQ-FUN-001 | 实现相互独立、可同时工作的全双工 UART 收发通道。 | 关键 |
| UART-DRQ-FUN-002 | 发送 1 位低起始、8 位 LSB 先发数据、1 位高停止、无校验的 8N1 帧。 | 关键 |
| UART-DRQ-FUN-003 | 接收通道以中点采样恢复 8N1 帧并输出有效字节完成指示。 | 关键 |
| UART-DRQ-FUN-004 | 停止位为低时报告帧错、丢弃错误字节并能在线路恢复后接收后续帧。 | 关键 |
| UART-DRQ-FUN-005 | 线路空闲和复位后 `txd` 为高电平。 | 重要 |

### 4.2 性能要求

| 编号 | 要求 | 关键度 |
|---|---|---|
| UART-DRQ-PERF-001 | 标称 9600 bit/s；本机误差不大于 ±0.5%，对端偏差 ±2% 时正确接收。 | 关键 |
| UART-DRQ-PERF-002 | 系统时钟按 100 MHz 设计，时钟与波特率应参数化。 | 重要 |
| UART-DRQ-PERF-003 | 连续吞吐率不低于 960 byte/s（10 bit/帧）。 | 重要 |

建立/保持时间、外部 I/O 时序和板级余量未提供，列入 UART-OQ-001。

### 4.3 算法要求

接收应从同步后的起始下降沿计时，在半位处确认起始位，其后每隔一个整位时间采样数据与停止位；发送分频取 `CLK_FREQ / BAUD_RATE` 的整数向下取整值。算法细化及误差预算见 UART-DOC-003。

## 5 接口关系

```mermaid
flowchart LR
  HOST[同步外部逻辑] <-->|tx_data/tx_start/tx_busy/tx_done<br/>rx_data/rx_done/frame_err| DUT[uart_top]
  DUT -->|txd 9600 8N1| LINE[异步串行线路]
  LINE -->|rxd 9600 8N1| DUT
```

| 编号 | 接口要求 |
|---|---|
| UART-DRQ-IF-001 | 发送接口含 `tx_data[7:0]`、`tx_start`、`tx_busy`、`tx_done`；忙时忽略新请求。 |
| UART-DRQ-IF-002 | 接收接口含 `rx_data[7:0]`、单拍 `rx_done` 和单拍 `frame_err`。 |
| UART-DRQ-IF-003 | 串行接口为一位 `txd` 输出和一位 `rxd` 输入；LOC/IOSTANDARD 待板级事实。 |
| UART-DRQ-IF-004 | 使用单一 `clk` 和同步高有效 `rst`。 |

## 6 关键性要求

### 6.1 可靠性

| 编号 | 要求 |
|---|---|
| UART-DRQ-REL-001 | 异步 `rxd` 进入功能逻辑前经过至少两级带 `ASYNC_REG` 的同步寄存器。 |
| UART-DRQ-REL-002 | 复位后状态确定；非法 FSM 状态下一拍恢复空闲。 |
| UART-DRQ-REL-003 | 帧错、毛刺和持续低电平不得导致死锁或伪造有效字节。 |

### 6.2 安全性

真实系统危害分析和 PLDS 完整性等级未提供。本节相应安全等级指标无内容；理由是缺少系统级输入，影响是不能确定独立性、覆盖率、故障注入和评审强度，须由系统安全责任人补充。

### 6.3 软件可编程要求

外部逻辑在发送空闲时以 `tx_start` 提交 `tx_data`，并在 `tx_done`、`rx_done`、`frame_err` 单拍有效时捕获结果；无地址映射寄存器或处理器总线接口。

### 6.4 保密性

本节无内容。项目未提供运行保密环境、类型或等级；该空缺不是“不适用”批准，数据分类须由授权角色决定。

### 6.5 其他要求

| 编号 | 要求 |
|---|---|
| UART-DRQ-TST-001 | 提供自检 TB，覆盖复位、格式、环回、帧错、容差等，失败 `$fatal`，全部成功打印 PASS。 |
| UART-DRQ-TST-002 | 支持 `txd` 直连 `rxd` 的环回验证。 |
| UART-DRQ-TST-003 | 每条 PLDSRS 需求规定验证方法并双向追踪。 |

## 7 设计约束

| 编号 | 约束 |
|---|---|
| UART-DRQ-DEV-002 | 纯 RTL，不使用厂商 IP、BRAM、DSP、MMCM/PLL。 |
| UART-DRQ-ENV-002 | RTL 使用 Verilog-2001 可综合子集，TB 可用 SystemVerilog；复位显式、无锁存、FSM 有 default 恢复。 |
| UART-DRQ-RES-001 | 候选预算：FF≤200、LUT≤300、BRAM/DSP=0；100 MHz 时序无负裕量。 |

管脚、Bank 电压、IOSTANDARD、配置电压和 I/O delay 未决；正式 `uart.xdc` 不降低 NSTD-1/UCIO-1，探索 XDC 不得进入门禁或发布。

## 8 管理要求

需求、设计、RTL 和测试候选应依次经过 G1～G4 的冻结快照、内容检查与人类批准；Agent 不得批准或建立基线。进度、组织和控制节点尚无合同批准数据，本节相应管理安排待项目负责人补充。

## 9 质量保证

### 9.1 标准

文档按 GJB 9764-2020 编制；其他开发、测试、约束标准须由质量角色取得受控版本后确定适用性。

### 9.2 文档

本项目至少维护 UART-DOC-000～008；正式文档须审查封面、修改页、目录、正文顺序、裁剪、引用、追踪和证据一致性。

### 9.3 配置管理

源码、TB、XDC 和文档使用唯一标识、版本和 SHA-256；运行绑定冻结输入，旧证据不得冒充当前版本证据。

### 9.4 测试要求

仿真使用独立的 PLDSSTP、PLDSSTD、PLDSSTR。配置项确认通常还需要 PLDSVTP、PLDSVTD、PLDSVTR，但当前因板级输入缺失而阻塞。

### 9.5 对分承制方要求

本节无内容。当前无已识别分承制方；如引入第三方，应补充其配置、质量、验证和交付要求。

## 10 验收与交付

| 编号 | 要求 |
|---|---|
| UART-DRQ-DLV-001 | 候选交付集含 RTL、TB、正式/探索用途隔离的 XDC、UART-DOC-000～008 和 manifest。 |
| UART-DRQ-DLV-002 | 全部交付物具有唯一标识、版本和哈希，以获批产品基线为准。 |

当前无验收程序、数量、介质、版权保护或批准产品基线，不能形成正式交付结论。

## 11 维护

问题应记录来源、影响、根因、修复和回归；RTL/TB/XDC 变化触发受影响分析和回归。验收后的改正性、适应性和完善性维护责任尚待合同或项目决定。

## 注释

开放问题：UART-OQ-001 为板级电气/时序事实缺失；UART-OQ-002 为危害分析和完整性等级缺失；UART-OQ-003 为除 GJB 9764 外其他适用标准的受控版本和逐条复核未完成。
